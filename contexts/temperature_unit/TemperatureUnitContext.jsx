// React Hook Imports
import { createContext } from "react";

const TemperatureUnitContext = createContext(); // Creating Temperature Unit Context

export default TemperatureUnitContext; // Exporting Temperature Unit Context
