// React Native Component Imports
import { Dimensions } from "react-native";

const screen_width = Dimensions.get("window").width; // Setting up a variable for screen width
const screen_height = Dimensions.get("window").height; // Setting up a variable for screen height

export { screen_width, screen_height };
